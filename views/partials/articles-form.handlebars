<!-- views/partials/articles-form.handlebars -->

<div class="form-group">
  <label for="title">Title</label>
  <input name="title" class="form-control" placeholder="Comedy: A prevalent theme in movies." value="{{article.title}}" />
</div>
<div class="form-group">
  <label for="desc">Description</label>
  <input name="desc" class="form-control" placeholder="Description text here." value="{{article.desc}}" />
</div>
<div class="form-group">
  <label for="body">Body</label>
  <input name="body" class="form-control" placeholder="Body text here." value="{{article.body}}" />
</div>
