<!-- views/partials/movies-form.handlebars -->

<div class="form-group">
  <label for="title">Title</label>
  <input name="title" class="form-control" placeholder="Inception" value="{{movie.title}}" />
</div>
<div class="form-group">
  <label for="desc">Description</label>
  <input name="desc" class="form-control" placeholder="A movie about dreaming inside of dreams... inside of dreams." value="{{movie.desc}}" />
</div>
<div class="form-group">
  <label for="imgUrl">Image Url</label>
  <input name="imgUrl" class="form-control" placeholder="" value="{{movie.imgUrl}}"/>
</div>
